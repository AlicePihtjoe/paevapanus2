const path = require('path');

module.exports = {
    cache: {
        type: 'filesystem',
        // Optional: You can set a custom cache directory
        // cacheDirectory: path.resolve(__dirname, '.cache/webpack')
    },
};
